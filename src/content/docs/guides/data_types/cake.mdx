---
title: Cake
description: Stack a color, an engine and a price layer into a complete car catalog, and reuse layers in a catalog for a second market.
---

import { Aside } from '@astrojs/starlight/components';
import Cake from '../../../../assets/figures/cake.svg';
import Snippet from '../../../../components/Snippet.astro';

<Cake
  width={120}
  role="img"
  aria-label="A cake: layers stacked over the same slices"
/>

A cake stacks layers into a complete catalog. All layers of a cake cover
the same slice ids, and each of them describes one aspect: in a car catalog,
the slices are the cars, and the layers give their colors, engines and
prices.

In this tutorial you build a car catalog for Germany with two cars, the
Taycan and the EX30, and three layers: colors, engines and prices. Then you
create a catalog for Switzerland that reuses the colors and the engines and
only changes the prices.

You will learn how to:

- share one set of slice ids between several layers
- stack layers into a cake
- reuse layers in another cake
- read what a cake says about each slice

## 1. Set up the project

Create a project and install the core package and the hashing package:

```bash
mkdir car-market
cd car-market
echo '{ "type": "module" }' > package.json
pnpm add @rljson/rljson @rljson/hash
```

`"type": "module"` lets the application use ES modules and top-level `await`.
Now create the file `catalog.ts`. Each of the following steps adds code to it:
put the imports of a step at the top of the file, and its code below the code
of the previous step. The complete file is shown at the end.

## 2. Create the components

The catalog describes colors, engines and prices. Each of them gets a
components table with a type of its own:

<Snippet
  file="cake.spec.ts"
  region="components"
  title="catalog.ts"
/>

## 3. Define the slices

The catalog has two slices, its cars. List their ids in a table of type
`sliceIds`:

<Snippet
  file="cake.spec.ts"
  region="slices"
  title="catalog.ts"
/>

## 4. Create a layer for each aspect

Each layer assigns a component to both cars. The three layers take their
components from different components tables, but they all use the same row
of slice ids, `models`. The helper `createLayer` makes sure of that:

<Snippet
  file="cake.spec.ts"
  region="layers"
  title="catalog.ts"
/>

The Taycan is white with 300 kW, the EX30 black with 200 kW.
[Layers](/guides/data_types/layers/) explains the fields of a layer in
detail.

## 5. Stack the layers into a catalog

A cake names its slice ids and maps the name of each layers table to the hash
of one layer in it:

<Snippet
  file="cake.spec.ts"
  region="cake"
  title="catalog.ts"
/>

| Field           | Meaning                                                   |
| --------------- | --------------------------------------------------------- |
| `sliceIdsTable` | The name of the table with the slice ids                  |
| `sliceIdsRow`   | The hash of the row that lists the slice ids of the cake  |
| `layers`        | Maps the name of each layers table to the hash of a layer |
| `id`            | Optional: the name of the cake                            |

Since `layers` is keyed by table, a cake takes at most one layer from each
layers table. That is why the colors, the engines and the prices each get a
layers table of their own. Note that the row of slice ids is called
`sliceIdsRow` here, while a layer calls it `sliceIdsTableRow`.

## 6. Create a catalog for Switzerland

Switzerland sells the same cars at different prices. Its catalog gets a new
price layer, and refers to the same color and engine layers as the catalog
for Germany:

<Snippet
  file="cake.spec.ts"
  region="variant"
  title="catalog.ts"
/>

## 7. Put the rows into tables

<Snippet
  file="cake.spec.ts"
  region="tables"
  title="catalog.ts"
/>

Apart from their ids, the two catalogs differ in one reference only: their
price layer. Both point to the same color layer and the same engine layer:

<Snippet
  file="catalogs.json"
  title="Output"
/>

This is how Rljson shares data: a variant stores what is new and refers to
everything else by hash. Nothing is copied.

## 8. Validate the catalogs

<Snippet
  file="cake.spec.ts"
  region="validate"
  title="catalog.ts"
/>

For every cake, `BaseValidator` checks that the table of slice ids and the
referenced row exist, and that every layers table exists and contains the
referenced layer. It also checks each layer, as described in
[Layers](/guides/data_types/layers/).

<Aside type="caution">
  The validator does not check that the layers of a cake use the slice ids of
  the cake. Keep them in sync yourself. Here, `createLayer` takes care of it.
</Aside>

## 9. Read the catalog

To describe a car, the application goes through the layers of the catalog.
For each layer, it looks up the layer in its table, and the component that
the layer assigns to the car in the layer's components table. `rowOf` finds
a row by its hash in any table of the car market, and `formats` turns a
component of each components table into words:

<Snippet
  file="cake.spec.ts"
  region="read"
  title="catalog.ts"
/>

The catalog's own row of slice ids tells the application which cars there
are. Like `add` in a layer, `layers` carries a `_hash` of its own, so the
application skips keys that start with an underscore.

## 10. See what the validator catches

Create a catalog for Austria that takes over the German prices with a price
layer of its own, but do not add the layer to the `priceLayers` table:

<Snippet
  file="cake.spec.ts"
  region="missing-layer"
/>

<Snippet
  file="missing-layer.json"
  title="Output"
/>

## 11. Run the application

Here is the complete `catalog.ts`:

<Snippet
  file="cake.spec.ts"
  region="app"
  title="catalog.ts"
/>

Run it with a Node.js version that runs TypeScript directly, like Node.js 24,
or with `npx tsx catalog.ts`:

```bash
node catalog.ts
```

<Snippet
  file="output.txt"
  title="Output"
/>

## What you have learned

- A cake names a row of slice ids and one layer from each of several layers
  tables.
- All layers of a cake assign their components to the same slice ids, and
  each of them describes one aspect of the catalog.
- A cake refers to its layers by hash, so another cake can reuse them.
- To read what a cake says about a slice, follow each layer of the cake to
  the component it assigns to that slice.

Next, a [Tree](/guides/data_types/tree/) stores hierarchical data.
