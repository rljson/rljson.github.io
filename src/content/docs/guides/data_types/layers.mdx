---
title: Layers
description: 'Describe one aspect of a car catalog with a layer: assign a color to every car, and derive the next model year from a base layer.'
---

import { Aside } from '@astrojs/starlight/components';
import Layer from '../../../../assets/figures/layer.svg';
import Snippet from '../../../../components/Snippet.astro';

<Layer
  width={120}
  role="img"
  aria-label="A layer cut into slices"
/>

To allow an efficient processing of thousands of objects we are introducing
the concept **layer**:

- Layers separate the data of complex objects into meaningful concerns
- Layers can be loaded and processed independently
- Layers can be updated using differences only

Examples:

- A price layer stores the prices of cars
- An engine layer stores the engines of cars
- A color layer stores the colors of cars

A single object in a layer is called a **slice**:

- A **slice** describes properties of a single object, e.g. a car
- A **layer** describes certain aspects of multiple slice, e.g. the price of multipe cars
- A **slice id** identifies a slice in a layer, e.g. the name of the car

In this tutorial you build the color layer of a car catalog. You describe the
colors of the cars of 2025, and then derive the catalog of 2026, which adds
the EX90 and drops the Macan.

You will learn how to:

- list slice ids in a `sliceIds` table
- create a layer that assigns components to slice ids
- derive slice ids and layers from a base, with `add` and `remove`
- resolve the assignments of a derived layer

## 1. Set up the project

Create a project and install the core package and the hashing package:

```bash
mkdir car-catalog
cd car-catalog
echo '{ "type": "module" }' > package.json
pnpm add @rljson/rljson @rljson/hash
```

`"type": "module"` lets the application use ES modules and top-level `await`.
Now create the file `colors.ts`. Each of the following steps adds code to it:
put the imports of a step at the top of the file, and its code below the code
of the previous step. The complete file is shown at the end.

## 2. Create the components

The components of the color layer are colors. Store them in a components
table:

<Snippet
  file="layers.spec.ts"
  region="colors"
  title="colors.ts"
/>

The helper `ref` reads the hash that `hip` has written into a row. Several
cars share a color: the Taycan and the EX30, for example, are both white.
Since equal rows have equal hashes, the table stores each color once. If
components tables are new to you, start with [Components](/guides/data_types/components/).

## 3. List the slice ids

The slice ids are the cars of the catalog. List the cars of 2025 in a row of
type `SliceIds`:

<Snippet
  file="layers.spec.ts"
  region="slice-ids"
  title="colors.ts"
/>

A row of slice ids has three fields:

| Field    | Meaning                                      |
| -------- | -------------------------------------------- |
| `base`   | The hash of a layer that this layer extends  |
| `add`    | The slice ids that are added to the base     |
| `remove` | The slice ids that are removed from the base |

Keeping the slice ids apart from the data pays off with large layers: a
client can fetch the slice ids first and load the components only for the
slices it needs.

## 4. Create a layer

The colors of 2025 are a layer. It assigns a color to each car:

<Snippet
  file="layers.spec.ts"
  region="layer"
  title="colors.ts"
/>

| Field              | Meaning                                                    |
| ------------------ | ---------------------------------------------------------- |
| `sliceIdsTable`    | The name of the table with the slice ids                   |
| `sliceIdsTableRow` | The hash of the row in that table that lists the slice ids |
| `componentsTable`  | The name of the table with the slice properties            |
| `add`              | The slices that are added to the layer                     |
| `base`             | Optional: the hash of a layer that this layer extends      |
| `remove`           | Optional: assignments of the base that this layer drops    |

A layer refers to tables by name
and to rows by their hashes. Like other rows, a layer may have an `id`; here
it names the model year.

## 5. Derive the next model year

In 2026 the EX90 is new and the Macan is gone. Instead of repeating the cars
of 2025, derive them: a new row of slice ids extends the cars of 2025 with
`ex90` and removes `macan`. A new layer extends the colors of 2025 with a
color for the EX90 and drops the Macan's color:

<Snippet
  file="layers.spec.ts"
  region="variants"
  title="colors.ts"
/>

The 2026 layer stores only what differs from its base. It follows one rule:
it assigns a component to every slice id that its own row of slice ids adds.
The row of 2026 adds `ex90`, so the layer assigns a color to `ex90`.

## 6. Put the rows into tables

Collect the rows of slice ids in a table of type `sliceIds` and the layers
in a table of type `layers`, and put all tables into an Rljson object:

<Snippet
  file="layers.spec.ts"
  region="tables"
  title="colors.ts"
/>

The table of slice ids shows how the rows build on each other:

<Snippet
  file="cars.json"
  title="Output"
/>

The layers table:

<Snippet
  file="color-layers.json"
  title="Output"
/>

`hip` has also hashed the objects in `add` and `remove`, so they carry a
`_hash` of their own. Keep that in mind when you iterate over them: keys that
start with an underscore are not slice ids.

## 7. Validate the catalog

<Snippet
  file="layers.spec.ts"
  region="validate"
  title="colors.ts"
/>

For every layer, `BaseValidator` checks that:

- the table of slice ids and the referenced row exist
- the components table exists
- every assignment points to an existing component
- every slice id of the row has an assignment
- the base layer exists in the same table, if there is one

## 8. Resolve the colors

To show the colors of a model year, the application resolves its layer: it
starts with the assignments of the base, adds the assignments of the layer
and drops the removed ones. Then it looks up the color of each car:

<Snippet
  file="layers.spec.ts"
  region="resolve"
  title="colors.ts"
/>

## 9. See what the validator catches

Create the 2026 layer again, but forget the color of the EX90:

<Snippet
  file="layers.spec.ts"
  region="missing-assignment"
/>

Its row of slice ids adds `ex90`, but the layer assigns nothing to it:

<Snippet
  file="missing-assignment.json"
  title="Output"
/>

<Aside type="note">
  The validator compares a layer with the `add` list of its own row of slice
  ids. So a derived layer either uses a derived row of slice ids, as in step 5,
  or it uses the row of its base and assigns all of its slice ids again.
</Aside>

## 10. Run the application

Here is the complete `colors.ts`:

<Snippet
  file="layers.spec.ts"
  region="app"
  title="colors.ts"
/>

Run it with a Node.js version that runs TypeScript directly, like Node.js 24,
or with `npx tsx colors.ts`:

```bash
node colors.ts
```

<Snippet
  file="output.txt"
  title="Output"
/>

## What you have learned

- A layer describes one aspect of a catalog, like the colors of the cars.
- A row of type `SliceIds` lists slice ids in `add`. It can extend a base row
  and remove slice ids from it.
- A layer names its table of slice ids, its row of slice ids and its
  components table, and assigns a component to each slice id in `add`.
- A derived layer extends a base layer with `add` and `remove` and stores
  only the difference.
- Equal components are stored once, however many slices use them.

Next, a [Cake](/guides/data_types/cake/) stacks the color, engine and price
layers into a complete catalog.
